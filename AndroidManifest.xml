<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.Converter">
    <uses-sdk android:minSdkVersion="18"/>
    <application android:icon="@drawable/icon" android:label="@string/app_name"
                 android:theme="@android:style/Theme.Holo.Light.NoActionBar">
        <activity android:name=".UnitChoice" android:label="unitChoice">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".Weight" android:label="weight"/>
        <activity android:name=".Distance" android:label="distance"/>
        <activity android:name=".Temperature" android:label="temperature"/>
    </application>
</manifest>
